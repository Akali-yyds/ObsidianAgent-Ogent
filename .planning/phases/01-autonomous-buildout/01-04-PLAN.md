---
phase: 01-autonomous-buildout
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/sessions.ts
  - src/main.ts
  - src/view.ts
  - styles.css
autonomous: true
requirements:
  - PACK-02
  - PACK-05
  - VERF-05
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "Every new chat starts in Classic mode, and selecting a pack changes only future turns in that session."
    - "Reopening a session restores its last selected pack and its last classic model value."
    - "Desktop users can select Grounded Research, see progress and verification results, and recover in-app from pack misconfiguration."
    - "Mobile users do not see unsupported multi-agent packs and get a safe Classic-mode recovery path instead of a crash."
    - "Pack results separate verified summary content from flagged claim details, including source-note navigation and model attribution."
  artifacts:
    - path: src/sessions.ts
      provides: Session-scoped pack and classic-model persistence
      contains: selectedPackId
    - path: src/view.ts
      provides: Mode selector, pack execution wiring, progress rendering, and claim UI
      contains: Open source note
    - path: styles.css
      provides: Pack header, progress stack, claim-card, and recovery banner styling
      contains: open-agent-claim
    - path: src/main.ts
      provides: Pack-aware ChatView dependencies
      contains: pack
  key_links:
    - from: src/view.ts
      to: src/sessions.ts
      via: selected pack and classic model persistence
      pattern: selectedPackId
    - from: src/view.ts
      to: src/packs/runtime.ts
      via: pack send path and progress/result handling
      pattern: runPack
    - from: src/view.ts
      to: Obsidian workspace file opening
      via: source-note action
      pattern: openFile
---

<objective>
Add the session-scoped pack selector and verification UI while keeping Classic mode behavior-safe by default.

Purpose: Expose the grounded-research capability inside the existing chat view without breaking the current single-agent UX.
Output: Session-aware pack state, mobile-safe gating, pack execution controls, and verified/flagged claim rendering in the chat panel.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-autonomous-buildout/01-CONTEXT.md
@.planning/phases/01-autonomous-buildout/01-UI-SPEC.md
@src/sessions.ts
@src/view.ts
@src/platform.ts
@styles.css

<interfaces>
From `src/sessions.ts`:
```ts
export interface SessionMeta {
  id: string;
  title: string;
  model: string;
  createdAt: number;
  updatedAt: number;
}
export interface StoredTurn {
  role: "user" | "assistant";
  content: string;
}
```

From `src/view.ts`:
```ts
export interface ChatViewDeps {
  getSettings: () => PluginSettings;
  openSettings: () => void;
  tools: ToolRegistry;
  consent: ConsentManager;
  undo: UndoBuffer;
  sessionStore: SessionStore;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist session-scoped pack state and structured pack turns</name>
  <files>src/sessions.ts, src/main.ts</files>
  <action>Extend session metadata and stored turn persistence to carry `selectedPackId`, the session’s last classic model value, and enough pack-turn metadata to re-render progress/results after a reopen. New sessions must start in Classic mode per D-01 and D-02, changing packs must only affect future turns per D-03, reopening must restore the session’s prior pack selection per D-04, and returning to Classic must restore that session’s last classic model value per D-08. Keep existing saved data backward-compatible so older sessions without pack fields still load cleanly.</action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>Session persistence supports per-session pack choice plus classic-model restoration without breaking existing stored session data.</done>
</task>

<task type="auto">
  <name>Task 2: Add the header mode selector, classic/pack gating, and recovery actions</name>
  <files>src/view.ts, styles.css, src/main.ts</files>
  <action>Add the header `Mode` control described in `01-UI-SPEC.md`, wiring it to session state rather than plugin settings per D-01 and D-02. When Classic is selected, preserve the current editable model input and send path; when a pack is selected, replace that input with read-only pack/provider/model information per D-05 and disable silent fallback on missing configuration per D-06. Surface in-app recovery actions (`Use Classic mode`, `Choose another pack`) for misconfigured packs per D-07, and hide unsupported multi-agent packs on mobile per PACK-05 while showing the required desktop-only recovery banner instead of crashing.</action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>The chat header supports session-scoped mode switching, preserves Classic behavior by default, and safely gates unsupported or misconfigured pack usage.</done>
</task>

<task type="auto">
  <name>Task 3: Render pack progress, verified summary, flagged claims, and source navigation</name>
  <files>src/view.ts, styles.css</files>
  <action>Wire pack-mode sends through the pack runtime and render the UI contract from `01-UI-SPEC.md`: compact 3-step progress, one retry status row, verified-summary prose, separate flagged-claims section, expandable claim cards, and `Open source note` actions. Apply the verification output policy exactly: verified summary contains only green claims per D-13, green requires quote present plus support per D-14, quote-present-but-unsupported claims show yellow warning details per D-15, and all-flagged results render the failure-style state with no clean summary per D-16. Include model attribution for retriever/synthesizer/verifier per UI-03, default verified cards collapsed, flagged cards expanded, and keep classic assistant rendering unchanged for non-pack turns.</action>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>Pack runs show progress and verification results inside the existing transcript, users can open the cited note, and verified/unsupported/quote-missing claims are visually distinct and policy-compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session data → chat view | Persisted pack selection and structured result data are rendered back into the UI |
| pack runtime → transcript renderer | Runtime progress/results become interactive UI elements and note-opening actions |
| UI action → workspace note open | User clicks open a vault file inside Obsidian |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | S | `src/view.ts` mode and recovery controls | mitigate | Drive mode from validated session state and explicit pack IDs only; do not infer pack activation from arbitrary turn content |
| T-01-04-02 | T | `src/view.ts` verification rendering | mitigate | Render verified/unsupported/quote-missing states from structured runtime fields, preserving D-13 through D-16 instead of deriving UI state from free-form markdown |
| T-01-04-03 | D | `src/view.ts` mobile pack gating | mitigate | Hide unsupported packs on mobile and disable the send path with a safe recovery banner so unsupported sessions cannot crash the view |
| T-01-04-04 | I | `src/view.ts` source-note action | mitigate | Resolve source navigation through Obsidian vault paths only and open local `TFile` instances rather than external URLs |
</threat_model>

<verification>
- `npm run build && npm run lint`
- Confirm Classic mode remains the default for new sessions and that pack-mode rendering stays inside the existing `src/view.ts` transcript/header layout.
</verification>

<success_criteria>
- Pack selection is session-scoped, opt-in, and restored on reopen.
- Classic mode remains behavior-safe by default.
- Pack results render policy-compliant verification UI with source-note navigation and model attribution.
- Mobile safely hides unsupported multi-agent pack usage.
</success_criteria>

<output>
After completion, create `.planning/phases/01-autonomous-buildout/01-04-SUMMARY.md`
</output>
