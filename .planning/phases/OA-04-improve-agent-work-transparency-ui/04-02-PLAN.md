---
phase: OA-04-improve-agent-work-transparency-ui
plan: "04-02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/citations.ts
  - src/packs/runtime.ts
  - tests/citations.test.ts
  - tests/packs/runtime.test.ts
autonomous: true
requirements:
  - UI-05
  - UI-06
user_setup: []
must_haves:
  truths:
    - "Successful grounded-research runs can produce a citation-ready `Research result` text backed only by exact anchors."
    - "Repeated use of the same exact anchored phrase reuses the same citation label, while different phrases get distinct labels."
    - "Absent, fuzzy-only, unsupported, or stale anchors never create fake inline exact citations."
  artifacts:
    - path: "src/citations.ts"
      provides: "Pure result-composition and citation-target resolution helpers"
      contains: "composeResearchResult"
    - path: "src/packs/runtime.ts"
      provides: "Runtime integration that populates citation-ready result fields"
      contains: "citations"
    - path: "tests/citations.test.ts"
      provides: "Coverage for label reuse, anchor filtering, and relocation fallback"
      contains: "occurrenceIndex"
    - path: "tests/packs/runtime.test.ts"
      provides: "Coverage for runtime result composition and safe absence paths"
      contains: "researchMarkdown"
  key_links:
    - from: "src/packs/runtime.ts"
      to: "src/citations.ts"
      via: "composition of `researchMarkdown` and citation map from anchored verified claims"
      pattern: "composeResearchResult"
    - from: "src/citations.ts"
      to: "StoredPackClaim.exactPhraseAnchor"
      via: "offset validation and occurrence-based relocation"
      pattern: "startOffset|occurrenceIndex"
---

<objective>
Turn verified anchored claims into the citation-ready result contract that the transcript redesign will render.

Purpose: Make inline citations deterministic and safe instead of heuristic.
Output: Citation composition/resolution helpers plus runtime population of `researchMarkdown` and ordered citation mappings.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OA-04-improve-agent-work-transparency-ui/04-UI-SPEC.md
@.planning/phases/OA-04-improve-agent-work-transparency-ui/04-RESEARCH.md
@.planning/phases/OA-04-improve-agent-work-transparency-ui/04-01-SUMMARY.md
@src/packs/runtime.ts
@src/agents/verifier.ts
@tests/packs/runtime.test.ts

<interfaces>
Expected from 04-01:
- Claim records can carry optional `exactPhraseAnchor`.
- Pack-turn/runtime contracts can carry optional `researchMarkdown` and `citations`.

Current runtime behavior:
- `verifiedSummary` remains available and should stay intact for compatibility.
- Citation-ready output must be added as a separate, richer result surface rather than overloading `verifiedSummary`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic result-composition and citation-resolution helpers</name>
  <files>src/citations.ts, tests/citations.test.ts</files>
  <behavior>
    - Test 1: verified claims with exact anchors compose prose-like `researchMarkdown` plus ordered inline citation links.
    - Test 2: identical `notePath + exactPhrase + occurrenceIndex` combinations reuse the same citation label.
    - Test 3: different phrases from the same note get different labels.
    - Test 4: unsupported, quote-missing, and fuzzy-only claims are excluded from inline citations.
    - Test 5: citation target resolution first validates stored offsets, then relocates by `exactPhrase + occurrenceIndex`, then returns an explicit fallback outcome.
  </behavior>
  <action>Create `src/citations.ts` as a pure Phase 4 helper module. Implement a deterministic `composeResearchResult()` that turns verified exact-anchor claims into prose-like `researchMarkdown` and an ordered citation array with stable labels such as `[1](openagent://citation/1)`. Reuse a label only when the underlying exact anchored phrase is the same occurrence in the same note; otherwise allocate a new label. Exclude unsupported, quote-missing, and fuzzy-only claims from inline citations entirely. In the same module, implement `resolveCitationTarget()` that validates the stored offsets against a live note body, relocates by `exactPhrase` plus `occurrenceIndex` when offsets drift, and returns a typed fallback when no exact target can be recovered.</action>
  <verify>
    <automated>npm test -- --run tests/citations.test.ts</automated>
  </verify>
  <done>The repo has pure, tested helpers that compose citation-ready result text and resolve exact phrase targets with safe fallback semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate runtime results with citation-ready research text and ordered citation mappings</name>
  <files>src/packs/runtime.ts, tests/packs/runtime.test.ts</files>
  <behavior>
    - Test 1: successful verified runs with exact anchors populate `researchMarkdown` and ordered `citations`.
    - Test 2: the same anchored phrase reused across claims reuses its citation label.
    - Test 3: runs with no exact anchors leave `researchMarkdown` and `citations` absent rather than fabricating unsupported citations.
    - Test 4: failed runs or verifier-disabled paths preserve current behavior without pretending a citation-ready result exists.
  </behavior>
  <action>Integrate the new citation helper into `runPackForEval()` and `runPack()` so grounded-research runs populate `researchMarkdown` and ordered `citations` from anchored verified claims while preserving `verifiedSummary` for backward compatibility. Keep the citation-ready fields absent when no trustworthy exact-anchor result can be composed. Do not generate citations from fuzzy matches, unsupported claims, or quote-missing claims. Ensure the runtime result contract now gives 04-03 everything it needs to render the `Research result` surface and exact-link click behavior without recomputing mappings in the view.</action>
  <verify>
    <automated>npm test -- --run tests/citations.test.ts tests/packs/runtime.test.ts</automated>
  </verify>
  <done>Runtime results now expose citation-ready research text and ordered citation mappings only when exact-anchor evidence supports them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier output → composed research text | Model-produced claim content becomes user-visible result prose |
| stored anchor metadata → citation target resolution | Persisted offsets are used to jump into live note content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | T | `src/citations.ts` label assignment | mitigate | Derive citation labels from anchored evidence only and reuse labels only for the same exact anchored phrase occurrence. |
| T-04-02-02 | I | `src/packs/runtime.ts` result composition | mitigate | Exclude unsupported, quote-missing, and fuzzy-only claims from inline citations so the primary result never overstates evidence quality. |
| T-04-02-03 | D | `resolveCitationTarget()` relocation | mitigate | Validate stored offsets first, attempt bounded relocation by exact phrase + occurrence index, and return a safe fallback outcome instead of throwing. |
</threat_model>

<verification>
- `npm test -- --run tests/citations.test.ts`
- `npm test -- --run tests/citations.test.ts tests/packs/runtime.test.ts`
</verification>

<success_criteria>
- Citation-ready `researchMarkdown` is composed from anchored verified claims.
- Citation labels are stable, ordered, and reused only when the same exact anchored phrase recurs.
- No exact anchor means no fake inline citation.
- Runtime results expose all data the UI needs for exact-jump links and stale-target fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/OA-04-improve-agent-work-transparency-ui/04-02-SUMMARY.md`
</output>
