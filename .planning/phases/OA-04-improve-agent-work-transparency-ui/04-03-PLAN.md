---
phase: OA-04-improve-agent-work-transparency-ui
plan: "04-03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/view.ts
  - styles.css
  - tests/view.test.ts
autonomous: true
requirements:
  - UI-04
  - UI-05
  - UI-06
  - UI-07
user_setup: []
must_haves:
  truths:
    - "Grounded-research turns reuse the existing Retriever, Synthesizer, and Verifier step blocks as the only transparency surface."
    - "The primary outcome surface is a `Research result` with inline citation links, or `Research result unavailable` when no citation-ready result exists."
    - "Clicking an inline citation jumps to the exact matched phrase when possible and falls back safely when the live note drifted."
    - "Classic and legacy turns remain safe when step details, anchors, or citation-ready fields are absent."
  artifacts:
    - path: "src/view.ts"
      provides: "Transcript rendering for clickable step rows, research result, and citation click handling"
      contains: "renderPackTurn"
    - path: "styles.css"
      provides: "Transcript-local styling for step disclosures, result metadata, and inline citation links"
      contains: "open-agent-pack-step"
    - path: "tests/view.test.ts"
      provides: "Regression coverage for UI removal, step expansion, inline citations, fallback behavior, and legacy safety"
      contains: "Research result"
  key_links:
    - from: "src/view.ts"
      to: "packTurn.progressSteps + packTurn.agentWork"
      via: "existing step rows upgraded into disclosures"
      pattern: "open-agent-pack-step"
    - from: "src/view.ts"
      to: "packTurn.researchMarkdown + packTurn.citations"
      via: "primary result rendering and citation click interception"
      pattern: "Research result"
    - from: "src/view.ts"
      to: "src/citations.ts"
      via: "offset validation, relocation, and safe citation fallback"
      pattern: "resolveCitationTarget"
---

<objective>
Replace the obsolete separate `Agent work` / `Run details` UI with the approved transcript redesign.

Purpose: Ship the real Phase 4 user experience: citation-ready result first, existing step rows as transparency, claim cards as fallback evidence.
Output: Reworked transcript renderer, citation navigation, styles, and regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OA-04-improve-agent-work-transparency-ui/04-UI-SPEC.md
@.planning/phases/OA-04-improve-agent-work-transparency-ui/04-RESEARCH.md
@.planning/phases/OA-04-improve-agent-work-transparency-ui/04-01-SUMMARY.md
@.planning/phases/OA-04-improve-agent-work-transparency-ui/04-02-SUMMARY.md
@src/view.ts
@src/citations.ts
@styles.css
@tests/view.test.ts

<interfaces>
From src/view.ts today:
- `renderPackTurn()` still renders a separate transparency surface via `renderPackTransparencyInfo()`.
- Step rows already exist as `.open-agent-pack-step`.
- `handlePackSend()` maps runtime results onto `assistantTurn.packTurn`.

Expected from 04-01 / 04-02:
- `packTurn.claims[]` may carry `exactPhraseAnchor`.
- `packTurn.researchMarkdown?` and `packTurn.citations?` hold the primary result and ordered citation mapping.
- `resolveCitationTarget()` can validate or relocate a stored citation target before the note is opened.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign pack-turn rendering so existing step rows become the only transparency surface</name>
  <files>src/view.ts, tests/view.test.ts</files>
  <behavior>
    - Test 1: completed grounded-research turns render `Research result` first, then the existing step stack, then claim cards, then the model footer.
    - Test 2: there is no rendered `Agent work`, `Run details`, or standalone run-metadata node.
    - Test 3: Retriever, Synthesizer, and Verifier rows are whole-row disclosure controls with one expanded row at a time.
    - Test 4: failed runs auto-expand the failed step on first render.
    - Test 5: Classic turns and legacy pack turns without step-detail or citation-ready fields render safely with pre-Phase-4 behavior.
    - Test 6: total timing appears near the `Research result` heading or step-stack header, not as a separate block.
    - Test 7: Retriever collapsed headers show note count plus up to 3 note-path chips, and expanded Retriever details show the brief plus note-path chips without raw retrieval scores.
    - Test 8: Synthesizer collapsed headers show draft-claim count plus summary preview, and expanded Synthesizer details show the summary plus the `Raw JSON` block.
    - Test 9: Verifier collapsed headers show the three status counts, and expanded Verifier details show a compact reason list keyed to claim order.
    - Test 10: every step header shows state plus step duration when known, uses the locked duration formatting rules, and falls back to `Timing unavailable` when timing data is absent.
  </behavior>
  <action>Remove the separate `renderPackTransparencyInfo()` pattern and upgrade the existing `.open-agent-pack-step` rows into the only transparency surface for grounded-research turns. Render grounded-research turns in the locked order: primary `Research result` surface (or `Research result unavailable` copy plus the locked no-result body copy), reused step stack, existing claim cards, model footer. Make each eligible step row a whole-row disclosure with `aria-expanded`, single-open behavior, and failed-step auto-expand on first render; keep live and completed turns on the same three-step structure. Use `agentWork` to populate the collapsed and expanded summaries for Retriever, Synthesizer, and Verifier inside those rows, and keep rows non-expandable only when they are purely pending with no detail payload. Lock the per-step detail contract in tests: Retriever collapsed note count + up to 3 chips and expanded brief-with-chips/no-scores, Synthesizer collapsed claim count + summary preview and expanded summary + `Raw JSON`, Verifier collapsed counts and expanded reason rows that include status, claim text preview, note basename, and verifier explanation in claim order. Also lock step-header state + per-step duration presentation, duration formatting rules, and `Timing unavailable` fallback. Preserve Classic turns and legacy stored pack turns by skipping the redesign when the required data is absent.</action>
  <verify>
    <automated>npm test -- --run tests/view.test.ts</automated>
  </verify>
  <done>The transcript no longer renders a separate transparency section, the existing step rows carry the transparency details, and legacy/classic safety is preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render inline citations, exact-jump behavior, and redesign styles with safe fallback</name>
  <files>src/view.ts, styles.css, tests/view.test.ts</files>
  <behavior>
    - Test 1: `researchMarkdown` renders as the primary result surface with ordered inline citation links.
    - Test 2: clicking a citation validates offsets, relocates by phrase + occurrence index when needed, and falls back to normal note open plus `Citation target no longer matches the live note.` when no exact target survives.
    - Test 3: repeated citation labels in the result resolve to the same target.
    - Test 4: claim cards remain the secondary evidence surface and still provide note-opening fallback.
    - Test 5: step rows meet the whole-row hit-target rule and keyboard focus works for interactive step rows and citation links.
    - Test 6: the view still renders safely when `researchMarkdown`, `citations`, or anchors are absent.
    - Test 7: clicking a retriever note chip opens the note only and does not also toggle the enclosing step-row disclosure.
    - Test 8: the Synthesizer `Raw JSON` block keeps the locked monospace, overflow, and height-cap behavior in the redesigned layout.
    - Test 9: `Research result unavailable` renders the locked fallback body copy when a citation-ready result cannot be composed.
  </behavior>
  <action>Render `researchMarkdown` as the primary answer surface and intercept its inline citation links using the ordered citation map from 04-02. On click, call `resolveCitationTarget()`, open the source note at the resolved exact phrase when possible, and if validation/relocation fails, open the note normally and show the locked fallback copy `Citation target no longer matches the live note.` Keep claim cards below the step stack as the secondary evidence surface with their current note-opening behavior. Ensure retriever note chips still open notes directly without double-triggering the enclosing step-row disclosure. Update `handlePackSend()` and stored-turn mapping so the new citation-ready fields persist onto `packTurn`. In `styles.css`, restyle the transcript around the approved Phase 4 contract: whole-row interactive step blocks, result metadata, inline citation links, expanded step detail panels, the locked `Raw JSON` monospace/overflow/height-cap behavior, and no separate transparency pane. Extend view tests to lock the removal of obsolete labels, citation rendering/click behavior, step-row expansion rules, failed-step auto-expand, step-header timing display/format/fallback, no-result body copy, keyboard/touch safety, retriever-chip event separation, and legacy-safe absence. Finish with a build verification.</action>
  <verify>
    <automated>npm test -- --run tests/view.test.ts && npm run build</automated>
  </verify>
  <done>Grounded-research turns now show the approved `Research result` plus inline citations, exact-jump behavior with safe fallback, reused step rows, reused claim cards, and passing UI/build regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted citation metadata → clickable transcript links | Stored offsets/phrases drive navigation into live vault notes |
| transcript interaction → vault file open and reveal | User clicks can navigate to note content inside Obsidian |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | T | `src/view.ts` citation click handling | mitigate | Resolve citation targets through validated offsets and bounded relocation; if no exact target survives, fall back to normal note open with the locked fallback notice. |
| T-04-03-02 | I | `src/view.ts` result rendering | mitigate | Render stored result text through safe text/markdown rendering and never invent inline citations when `citations` or anchors are absent. |
| T-04-03-03 | D | step-row disclosure UI | mitigate | Keep the same three-step stack for live and final states, use one-expanded-step state, and avoid extra panes or nested transparency trees that could break transcript usability. |
</threat_model>

<verification>
- `npm test -- --run tests/view.test.ts`
- `npm test -- --run tests/agents/quote-match.test.ts tests/agents/verifier.test.ts tests/packs/runtime.test.ts tests/sessions.test.ts tests/view.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Grounded-research turns show `Research result` first and never render a separate `Agent work` or `Run details` section.
- Existing Retriever, Synthesizer, and Verifier rows are the only transparency surface and are whole-row interactive.
- Inline citations jump to exact phrase targets when possible and fall back safely when note text drifted.
- Claim cards remain below the step stack as the secondary evidence surface.
- Classic and legacy turns remain stable when Phase 4 data is absent.
</success_criteria>

<output>
After completion, create `.planning/phases/OA-04-improve-agent-work-transparency-ui/04-03-SUMMARY.md`
</output>
