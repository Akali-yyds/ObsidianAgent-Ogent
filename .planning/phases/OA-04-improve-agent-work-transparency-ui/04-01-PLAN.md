---
phase: OA-04-improve-agent-work-transparency-ui
plan: "04-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/quote-match.ts
  - src/agents/verifier.ts
  - src/packs/runtime.ts
  - src/sessions.ts
  - tests/agents/quote-match.test.ts
  - tests/agents/verifier.test.ts
  - tests/packs/runtime.test.ts
  - tests/sessions.test.ts
autonomous: true
requirements:
  - UI-06
  - UI-07
user_setup: []
must_haves:
  truths:
    - "Exact live-note phrase matches persist stable anchors with offsets and occurrence indexing."
    - "Fuzzy quote matches can still support verification outcomes but never masquerade as exact inline citations."
    - "Grounded-research turns can carry optional citation-ready fields without breaking legacy or Classic session reads."
  artifacts:
    - path: "src/agents/quote-match.ts"
      provides: "Structured quote-resolution result with exact/fuzzy/missing states"
      contains: "exactPhrase"
    - path: "src/agents/verifier.ts"
      provides: "Claim verification records with optional exact-phrase anchor data"
      contains: "exactPhraseAnchor"
    - path: "src/packs/runtime.ts"
      provides: "Pack result contracts extended for citation-ready turn data"
      contains: "researchMarkdown"
    - path: "src/sessions.ts"
      provides: "Legacy-safe stored turn schema and sanitizers for anchors/citations"
      contains: "citations"
  key_links:
    - from: "src/agents/verifier.ts"
      to: "src/agents/quote-match.ts"
      via: "structured quote resolution during live-note verification"
      pattern: "exactPhraseAnchor"
    - from: "src/packs/runtime.ts"
      to: "src/sessions.ts"
      via: "optional result/session contracts for citation-ready grounded-research turns"
      pattern: "researchMarkdown|citations"
---

<objective>
Replace the obsolete transparency-only Phase 4 data plan with exact-anchor and citation-ready contracts.

Purpose: Make citation correctness the foundation of the redesign instead of treating it as a later UI detail.
Output: Quote-resolution anchors, verifier/runtime/session contracts, and regression coverage that keeps legacy turns safe.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OA-04-improve-agent-work-transparency-ui/04-UI-SPEC.md
@.planning/phases/OA-04-improve-agent-work-transparency-ui/04-RESEARCH.md
@src/agents/quote-match.ts
@src/agents/verifier.ts
@src/packs/runtime.ts
@src/sessions.ts
@tests/agents/quote-match.test.ts
@tests/agents/verifier.test.ts
@tests/packs/runtime.test.ts
@tests/sessions.test.ts

<interfaces>
From src/agents/quote-match.ts:
- `quotePresent(noteBody, quote): boolean` currently answers only yes/no.
- Phase 4 needs a richer resolution result: exact anchor, fuzzy-only match, or missing.

From src/agents/verifier.ts:
- `ClaimVerification` currently stores status/support text but no exact anchor.
- `verifyClaims()` reads the live note body, so this is the correct place to capture anchors.

From src/packs/runtime.ts:
- `PackRunResult` currently returns `verifiedSummary`, `claims`, `modelsUsed`, `artifacts`, and `transparency`.
- Phase 4 needs optional citation-ready turn fields alongside the existing payload.

From src/sessions.ts:
- `StoredPackTurnData` and `StoredPackClaim` currently persist claim text and quote text only.
- Sanitizers already fail soft on malformed optional nested data; preserve that behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture exact phrase anchors during quote resolution and verification</name>
  <files>src/agents/quote-match.ts, src/agents/verifier.ts, tests/agents/quote-match.test.ts, tests/agents/verifier.test.ts</files>
  <behavior>
    - Test 1: exact live-note matches return preserved `exactPhrase`, `startOffset`, `endOffset`, and `occurrenceIndex`.
    - Test 2: repeated phrases choose the correct occurrence index instead of the first duplicate span.
    - Test 3: fuzzy-only quote matches still count as quote-present for verification flow but return no exact anchor.
    - Test 4: missing files or unresolved quotes still produce `quote-missing` verification results.
    - Test 5: preserved `exactPhrase` keeps live casing and punctuation from the note body rather than normalized quote text.
  </behavior>
  <action>Refactor `src/agents/quote-match.ts` to expose a structured quote-resolution result instead of only a boolean, while keeping `quotePresent()` as a compatibility wrapper where that helps contain churn. Distinguish `exact`, `fuzzy`, and `missing` outcomes; for `exact`, capture `notePath`, `exactPhrase`, `startOffset`, `endOffset`, and `occurrenceIndex` from the live note body. Update `verifyClaims()` to use the structured result, attach `exactPhraseAnchor` only when the resolution is exact, and keep fuzzy matches eligible for verification support decisions without ever creating an inline-citation-ready anchor.</action>
  <verify>
    <automated>npm test -- --run tests/agents/quote-match.test.ts tests/agents/verifier.test.ts</automated>
  </verify>
  <done>Verifier results preserve exact anchors for exact matches, keep fuzzy matches citation-ineligible, and all quote/verifier tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend runtime and stored-turn contracts for anchored citation-ready turns with legacy-safe sanitizing</name>
  <files>src/packs/runtime.ts, src/sessions.ts, tests/packs/runtime.test.ts, tests/sessions.test.ts</files>
  <behavior>
    - Test 1: runtime result contracts expose optional `researchMarkdown`, `citations`, and anchored-claim fields without breaking existing result consumers.
    - Test 2: stored turns round-trip claims that include exact anchors.
    - Test 3: malformed nested anchor or citation payloads are dropped on load while the rest of the turn remains readable.
    - Test 4: legacy pack turns with no new fields still deserialize unchanged.
  </behavior>
  <action>Extend grounded-research runtime and storage contracts to carry the data Phase 4 needs: optional `exactPhraseAnchor` on stored/runtime claim records, plus optional top-level `researchMarkdown` and ordered `citations` fields for the assistant pack turn. Keep every new field optional so pre-redesign sessions and Classic turns remain valid. Sanitize malformed anchor/citation payloads out of `loadStoredTurnsFile()` rather than migrating or crashing them. Define the contract names and shapes now so 04-02 can populate them deterministically from verified anchors; do not invent fallback prose in this plan.</action>
  <verify>
    <automated>npm test -- --run tests/packs/runtime.test.ts tests/sessions.test.ts</automated>
  </verify>
  <done>Runtime/session contracts can hold exact-anchor and citation-ready fields, malformed nested payloads fail soft, and legacy turns stay readable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live note text → stored anchor data | Untrusted vault content becomes persisted citation metadata |
| persisted session JSON → transcript renderer | Stored nested payloads are rendered back into the Obsidian UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | T | `src/agents/quote-match.ts` anchor capture | mitigate | Persist anchors only from verified exact substring spans in the live note; fuzzy matches stay explicit and anchorless. |
| T-04-01-02 | I | `src/sessions.ts` stored turn payload | mitigate | Keep anchor/citation fields optional and sanitize malformed nested data instead of trusting persisted JSON blindly. |
| T-04-01-03 | D | legacy session loading | mitigate | Preserve current soft-failure load path so old turns without anchors or result fields do not break transcript rendering. |
</threat_model>

<verification>
- `npm test -- --run tests/agents/quote-match.test.ts tests/agents/verifier.test.ts`
- `npm test -- --run tests/packs/runtime.test.ts tests/sessions.test.ts`
</verification>

<success_criteria>
- Exact quote matches now produce persisted phrase anchors with offsets and occurrence index.
- Fuzzy-only matches remain usable for verification status but cannot become inline citations.
- Grounded-research result/session contracts can hold citation-ready fields.
- Legacy sessions and malformed optional payloads still load safely.
</success_criteria>

<output>
After completion, create `.planning/phases/OA-04-improve-agent-work-transparency-ui/04-01-SUMMARY.md`
</output>
